/*
#include<iostream>

using namespace std;

const char* quality[4] = {"Spring","Summer","autumn","Winter"};

int main()
{
	int index;
	int month;
	cout << "Enter the month : ";
	cin >> month;
	if (month >= 1 && month <= 3)
	{
		index = 0;
	}
	else if (month >= 4 && month <= 6)
	{
		index = 1;
	}
	else if (month >= 7 && month <= 9)
	{
		index = 2;
	}
	else
	{
		index = 3;
	}
	cout << "The " << month << "th month belongs to " << quality[index] << endl;
	return 0;
}
*/
